// l1 := import(":non-existent-library-1")
// comments should be skipped by compiler

   /*
   multiline comments also should be ignored
l2 := import(":non-existent-library-2")
   */

complex := import(":complex-lib") // check we find 'index.lib.tengo' when importing a directory
