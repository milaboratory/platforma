// Workflow entry point for ${shortName}.
// Author owns this file; the structurer only seeds it at init time.

wf := import("@platforma-sdk/workflow-tengo:workflow")

wf.body(func(args) {
  return {
    outputs: {},
    exports: {}
  }
})
