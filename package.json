{
  "name": "@milaboratories/platforma-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:local": "PL_PKG_DEV=local pnpm run build",
    "test": "turbo run test --concurrency 1",
    "test:local": "PL_PKG_DEV=local node --run test",
    "test:local,dry-run": "PL_PKG_DEV=local node --run test -- --dry-run=json",
    "test:local,no-cache": "PL_PKG_DEV=local node --run test -- --force",
    "do-pack": "turbo run do-pack",
    "do-pack:local": "PL_PKG_DEV=local turbo run do-pack",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "prepare": "husky",
    "fmt": "turbo run fmt",
    "check": "turbo run check",
    "formatter:check": "turbo run formatter:check",
    "linter:check": "turbo run linter:check",
    "types:check": "turbo run types:check",
    "ci:prepare": "pnpm --filter \"@milaboratories/ts-builder...\" run build && pnpm run formatter:check --output-logs=errors-only && pnpm run linter:check --output-logs=errors-only",
    "ci:build": "pnpm run ci:prepare && pnpm run build --output-logs=errors-only",
    "ci:build:local": "pnpm run ci:prepare && pnpm run build:local --output-logs=errors-only",
    "ci:test": "pnpm run test --output-logs=errors-only",
    "ci:test:local": "pnpm run test:local --output-logs=errors-only",
    "ci:test:local,no-cache": "pnpm run test:local,no-cache --output-logs=errors-only",
    "reset": "pnpm run reset:dist && pnpm run reset:node_modules && pnpm run reset:turbo-cache",
    "reset:dist": "find . -type d -name 'dist' | grep -v 'node_modules' | xargs rm -rf",
    "reset:etc-blocks": "rimraf etc/blocks/**/.turbo !**/.pnpm/** etc/blocks/**/ui/dist etc/blocks/**/model/dist --glob",
    "reset:turbo-cache": "find . -type d -name '.turbo' | grep -v '\\.pnpm' | xargs rm -r",
    "reset:node_modules": "find . -type d -name 'node_modules' | grep -v '\\.pnpm' | xargs rm -r"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@types/node": "catalog:",
    "@zed-industries/vscode-langservers-extracted": "catalog:",
    "husky": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "rimraf": "catalog:",
    "rollup": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.14.4+sha512.c8180b3fbe4e4bca02c94234717896b5529740a6cbadf19fa78254270403ea2f27d4e1d46a08a0f56c89b63dc8ebfd3ee53326da720273794e6200fcf0d184ab",
  "pnpm": {
    "overrides": {
      "@microsoft/api-extractor>typescript": "catalog:"
    }
  },
  "//": {
    "@platforma-open/milaboratories.software-ptabler": "file:/Users/dbolotin/mi/core/ptabler/software/package.tgz",
    "overrides": {}
  }
}
