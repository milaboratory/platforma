/** Runs a cat on the file and returns it's content as a value resource. */

self := import("@platforma-sdk/workflow-tengo:tpl")
assets := import("@platforma-sdk/workflow-tengo:assets")
smart := import("@platforma-sdk/workflow-tengo:smart")
exec := import("@platforma-sdk/workflow-tengo:exec")
file := import("@platforma-sdk/workflow-tengo:file")

self.defineOutputs(["main"])

// sw := assets.importSoftware("@platforma-open/milaboratories.software-anarci:main")
sw := assets.importSoftware("@platforma-open/milaboratories.software-conda-empty:main")

self.body(func(inputs) {
	run := exec.builder().
		software(sw).
		inLightQueue().
		arg("--version").
		saveStdoutContent().
		run()

	result := {
		main: run.getStdoutContent()
	}

	return result
})
