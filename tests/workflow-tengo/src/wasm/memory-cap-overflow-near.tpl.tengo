// Allocation just above the live 32 MiB cap (40 MiB). The guest's
// allocator hits the per-instance memory limit and traps; the bridge
// aborts the script.

self := import("@platforma-sdk/workflow-tengo:tpl")
assets := import("@platforma-sdk/workflow-tengo:assets")

self.defineOutputs(["unreachable"])

self.body(func(_inputs) {
	api := assets.importWasm("@platforma-tests/wasm-table-fixture:main")["demo:tables/api"]
	api.table.consumeMemory(string(40 * 1024 * 1024))
	return { unreachable: "reached end of body v2" }
})
