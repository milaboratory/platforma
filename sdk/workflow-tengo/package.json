{
  "name": "@platforma-sdk/workflow-tengo",
  "version": "1.2.7",
  "description": "Tengo SDK library",
  "files": [
    "./dist/**/*",
    "./index.d.ts",
    "./index.js"
  ],
  "scripts": {
    "type-check": "pl-tengo check",
    "build": "rm -rf dist && pl-tengo check && pl-tengo build --tags-file=../.tags",
    "tengo-test": "rm -rf dist && pl-tengo check && pl-tengo build --tags-file=../.tags && pl-tengo test",
    "test": "rm -rf dist && pl-tengo check && pl-tengo build --tags-file=../.tags && pl-tengo test && vitest",
    "format": "/usr/bin/env emacs --quick --script ./format.el",
    "format-stdin": "/usr/bin/env emacs --quick --script ./format-stdin.el"
  },
  "dependencies": {
    "@milaboratories/software-pframes-conv": "catalog:"
  },
  "devDependencies": {
    "@milaboratories/tengo-template-builder": "workspace:^"
  }
}
