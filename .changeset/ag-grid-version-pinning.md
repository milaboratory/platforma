---
"@platforma-sdk/blocks-deps-updater": minor
"@platforma-sdk/block-tools": patch
---

Rewrite blocks-deps-updater as a TypeScript package with vite build, add ag-grid version pinning to ~34.1.2, and update block-tools to import directly instead of spawning a subprocess.
