---
"@platforma-sdk/workflow-tengo": minor
---

Add `pframes.labels` module with `deriveDistinctLabels` — trace-based label disambiguation for PColumn specs. Port of the TypeScript `deriveDistinctLabels` from `@platforma-sdk/model`. Also adds `A_IS_LINKER` annotation constant and `isLinkerColumn` helper to `pframes.spec`.
