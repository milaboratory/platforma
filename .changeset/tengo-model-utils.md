---
"@platforma-sdk/workflow-tengo": minor
---

Add `pframes.labels` module with `deriveDistinctLabels` — trace-based label disambiguation for PColumn specs. Port of the TypeScript `deriveDistinctLabels` from `@platforma-sdk/model`.
