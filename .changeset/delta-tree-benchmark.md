---
"@milaboratories/pl-tree": minor
---

Add a cost benchmark comparing the three tree loading algorithms across pruning on/off, and
finalisation on/off via `PL_TREE_NO_FINALISATION`. Skipped unless `PL_TREE_BENCH=1`.
