---
"@milaboratories/pl-tree": patch
---

Add a differential test asserting all three tree loading algorithms converge on an identical
mirror after every step of a mutation sequence, as the backwards-compatibility guard for
delta becoming the default.
