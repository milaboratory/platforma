# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
  title: Progress API
  description: Progress provides access to progress of any long-running process associated with resource.
  version: 0.0.1
paths:
  /v1/get-progress:
    post:
      tags:
        - Progress
      operationId: Progress_GetStatus
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetStatus_Request"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetStatus_Response"
        default:
          description: Default error response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Status"
  /v1/realtime-progress:
    post:
      tags:
        - Progress
      operationId: Progress_RealtimeStatus
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RealtimeStatus_Request"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RealtimeStatus_Response"
        default:
          description: Default error response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Status"
components:
  schemas:
    GetStatus_Request:
      type: object
      properties:
        resourceId:
          type: string
    GetStatus_Response:
      type: object
      properties:
        report:
          $ref: "#/components/schemas/ProgressAPI_Report"
    GoogleProtobufAny:
      type: object
      properties:
        "@type":
          type: string
          description: The type of the serialized message.
      additionalProperties: true
      description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
    ProgressAPI_Report:
      type: object
      properties:
        progress:
          type: number
          format: float
        bytesProcessed:
          type: string
        bytesTotal:
          type: string
        done:
          type: boolean
        name:
          type: string
          description: Name of current progress stage (if any)
    RealtimeStatus_Request:
      type: object
      properties:
        resourceId:
          type: string
        updateInterval:
          pattern: ^-?(?:0|[1-9][0-9]{0,11})(?:\.[0-9]{1,9})?s$
          type: string
    RealtimeStatus_Response:
      type: object
      properties:
        report:
          $ref: "#/components/schemas/ProgressAPI_Report"
    Status:
      type: object
      properties:
        code:
          type: integer
          description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
          format: int32
        message:
          type: string
          description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
        details:
          type: array
          items:
            $ref: "#/components/schemas/GoogleProtobufAny"
          description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
      description: "The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors)."
tags:
  - name: Progress
