// import { getPrivateKey, SSH_TEST_HOST, SSH_TEST_USER } from './connections.secret';
// import { forwardMultiplePorts } from './ssh';

// const config = {
//   host: SSH_TEST_HOST,
//   username: SSH_TEST_USER,
//   privateKey: getPrivateKey(),
// };
// forwardMultiplePorts(config, { remotePort: 1415, localPort: 1415 });

// forwardMultiplePorts(config, { remotePort: 42097, localPort: 42097 });

// // forwardMultiplePorts(config, { remotePort: 37659, localPort: 37659 });

// // forwardMultiplePorts(config, { remotePort: 39841, localPort: 39841 });

// // forwardMultiplePorts(config, { remotePort: 9000, localPort: 9000 });

// // forwardMultiplePorts({
// //   host: SSH_TEST_HOST,
// //   username: SSH_TEST_USER,
// //   privateKey: getPrivateKey(),
// // }, { remotePort: 9001, localPort: 9001 });
